define service {
		hostgroup_name 					vm_instances
        service_description             VM CPU Usage
        check_command                   check_libvirt_kvm_guest_cpu
        use                             generic-service
        notification_interval           0 ; set > 0 if you want to be renotified
}
define service {
		hostgroup_name 					vm_instances
        service_description             VM Mem Usage
        check_command                   check_libvirt_kvm_guest_mem
        use                             generic-service
        notification_interval           0 ; set > 0 if you want to be renotified
}
define service {
		hostgroup_name 					vm_instances
        service_description             VM Disk IO Usage
        check_command                   check_libvirt_kvm_guest_disk_io
        use                             generic-service
        notification_interval           0 ; set > 0 if you want to be renotified
}
define service {
		hostgroup_name 					vm_instances
        service_description             VM Net IO Usage
        check_command                   check_libvirt_kvm_guest_net_io
        use                             generic-service
        notification_interval           0 ; set > 0 if you want to be renotified
}
