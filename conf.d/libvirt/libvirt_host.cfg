define service {
		hostgroup_name 					libvirt-servers
        service_description             KVM running vm list
        check_command                   check_libvirt_kvm_host_running
        use                             generic-service
        notification_interval           0 ; set > 0 if you want to be renotified
}
